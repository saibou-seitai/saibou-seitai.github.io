# タスク一覧

## 公開URL確定後のSEO対応

- 状態: 保留
- 待ち事項: GitHub Pagesの公開URLまたは独自ドメイン
- 対象: `docs` 内のデジタル名刺サイト

公開URLの連絡を受けたら、次の対応を行います。

- `sitemap.xml`を作成する
- `robots.txt`を作成する
- canonical URLを設定する
- Open GraphとX向けのメタデータを設定する
- JSON-LD構造化データを追加する
- sitemap、canonical、メタデータ、構造化データに確定した絶対URLを使用する
- 公開後に各ファイルとHTMLの出力を確認する

公開URLは推測せず、確定情報が届くまでURL依存の実装を行いません。

