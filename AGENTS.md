# デジタル名刺サイト運用ルール

## プロジェクト概要

- 渡邉和明（食善師）のデジタル名刺サイトです。
- 公開サイトは、シンプルな和モダンのデザインを維持してください。
- 顔写真、SNSリンク、「活動の記録」「今後の予定」を主要コンテンツとします。

## 公開方法

- GitHub Pagesで公開します。
- GitHub Pagesの公開元は、リポジトリのデフォルトブランチにある `/docs` フォルダです。
- 公開ページの入口は `/docs/index.html` です。
- 公開前に、GitHubの `Settings > Pages` で `Deploy from a branch` を選び、デフォルトブランチの `/docs` を指定してください。

## 実装ルール

- 使用できる技術はHTML、CSS、JavaScriptのみです。
- サーバーサイド処理、データベース、ビルドを必要とするフレームワークは使用しません。
- 外部ライブラリやCDNは、明確な必要性がない限り追加しません。
- PCとスマートフォンの両方で、横スクロールや文字切れが発生しないようにします。
- SNSリンクは新しいタブで開き、`rel="noopener noreferrer"` を付けます。
- 活動内容や予定は、確認できていない情報を推測して追加しません。

## ファイル管理

- 公開に必要なファイルだけを `/docs` に置きます。
- 制作途中の案、比較ページ、一時ファイル、確認用画像、ログ、依存パッケージは `/docs` に置きません。
- 顔写真は `/docs/assets/profile.png` を使用します。
- 顔写真の色味を変えるフィルターは適用しません。
- 現在の主要ファイルは次のとおりです。
  - `/docs/index.html`
  - `/docs/style.css`
  - `/docs/script.js`
  - `/docs/assets/profile.png`

## タスク管理

- 未完了の作業は、リポジトリ直下の `/TASKS.md` で管理します。
- 仕様、決定事項、確定したURL、運用上の注意など、今後参照する情報は `/PROJECT_MEMORY.md` に記録します。
- 作業ルールは `/AGENTS.md`、未完了タスクは `/TASKS.md`、継続して参照するプロジェクト情報は `/PROJECT_MEMORY.md` を正とします。
- 記憶すべき情報は、すべてリポジトリ内のMarkdownファイルに保存します。
- 端末固有の記憶、Codex内部の記憶、会話履歴、ローカルキャッシュだけに情報を保存しません。
- 別の端末や新しい作業環境でも引き継げるよう、作業終了前に必要な情報をMarkdownへ反映します。
- 作業開始時は `/AGENTS.md`、`/TASKS.md`、`/PROJECT_MEMORY.md` を確認します。
- タスクが完了したら、確認結果を記録してから該当項目を完了状態に更新します。

## Markdown記録の更新基準

- ユーザーが「記憶して」「今後も使う」「保留」「あとで対応」と指定した内容は、その場で該当するMarkdownへ記録します。
- 推測や未確認情報は確定事項として記録せず、保留または未確認と明記します。
- 情報が変更された場合は、古い内容を残したまま追加するのではなく、該当箇所を更新して現在の状態を明確にします。
- 公開URLやSNS URLなど外部環境に依存する情報は、確認日と確認元が必要な場合に併記します。

## 更新時の確認

- HTML内のローカルファイル参照が存在することを確認します。
- JavaScriptの構文エラーがないことを確認します。
- Instagram、X、TikTok、Facebookのリンク先を確認します。
- 顔写真が元の色で表示されることを確認します。
- デスクトップ幅とスマートフォン幅の両方で表示を確認します。
